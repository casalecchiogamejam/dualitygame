using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SphereEffect : MonoBehaviour
{
    
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    // teniamo una box volume sempre attiva in scena che rappresenta il mondo attuale
    // l'animazione di switch scala una box e alla fine dell'animazione va a settare la box attuale con quella dell'animazione corrente
}
